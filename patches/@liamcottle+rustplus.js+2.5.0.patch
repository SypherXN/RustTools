diff --git a/node_modules/@liamcottle/rustplus.js/camera.js b/node_modules/@liamcottle/rustplus.js/camera.js
index 85765d5..5a8e85d 100644
--- a/node_modules/@liamcottle/rustplus.js/camera.js
+++ b/node_modules/@liamcottle/rustplus.js/camera.js
@@ -268,12 +268,12 @@ class Camera extends EventEmitter {
 
     async _subscribe() {
 
-        // subscribe to camera
+        // subscribe to camera (CCTV can be slow on first connect; allow longer than default 10s)
         const response = await this.rustplus.sendRequestAsync({
             cameraSubscribe: {
                 cameraId: this.identifier,
             },
-        });
+        }, 45_000);
 
         // update camera subscribe info
         this.cameraSubscribeInfo = response.cameraSubscribeInfo;
@@ -320,7 +320,7 @@ class Camera extends EventEmitter {
                     cameraUnsubscribe: {
 
                     },
-                });
+                }, 15_000);
             } catch (error) {
                 // ignore errors unsubscribing from camera
             }
diff --git a/node_modules/@liamcottle/rustplus.js/rustplus.js b/node_modules/@liamcottle/rustplus.js/rustplus.js
index 4497096..73128f5 100644
--- a/node_modules/@liamcottle/rustplus.js/rustplus.js
+++ b/node_modules/@liamcottle/rustplus.js/rustplus.js
@@ -74,8 +74,14 @@ class RustPlus extends EventEmitter {
 
             this.websocket.on('message', (data) => {
 
-                // decode received message
-                var message = this.AppMessage.decode(data);
+                // decode received message (proto may omit fields the game does not send)
+                var message;
+                try {
+                    message = this.AppMessage.decode(data);
+                } catch (e) {
+                    this.emit('error', e);
+                    return;
+                }
 
                 // check if received message is a response and if we have a callback registered for it
                 if(message.response && message.response.seq && this.seqCallbacks[message.response.seq]){
@@ -97,7 +103,7 @@ class RustPlus extends EventEmitter {
                 }
 
                 // fire message event for received messages that aren't handled by callback
-                this.emit('message', this.AppMessage.decode(data));
+                this.emit('message', message);
 
             });
 
diff --git a/node_modules/@liamcottle/rustplus.js/rustplus.proto b/node_modules/@liamcottle/rustplus.js/rustplus.proto
index d893865..2b5822d 100644
--- a/node_modules/@liamcottle/rustplus.js/rustplus.proto
+++ b/node_modules/@liamcottle/rustplus.js/rustplus.proto
@@ -229,7 +229,7 @@ message AppInfo {
 	required uint32 wipeTime = 6;
 	required uint32 players = 7;
 	required uint32 maxPlayers = 8;
-	required uint32 queuedPlayers = 9;
+	optional uint32 queuedPlayers = 9;
 	optional uint32 seed = 10;
 	optional uint32 salt = 11;
 	optional string logoImage = 12;
@@ -276,7 +276,7 @@ message AppEntityPayload {
 	message Item {
 		required int32 itemId = 1;
 		required int32 quantity = 2;
-		required bool itemIsBlueprint = 3;
+		optional bool itemIsBlueprint = 3;
 	}
 }
 
@@ -289,16 +289,16 @@ message AppTeamInfo {
 	message Member {
 		required uint64 steamId = 1;
 		required string name = 2;
-		required float x = 3;
-		required float y = 4;
-		required bool isOnline = 5;
-		required uint32 spawnTime = 6;
-		required bool isAlive = 7;
-		required uint32 deathTime = 8;
+		optional float x = 3;
+		optional float y = 4;
+		optional bool isOnline = 5;
+		optional uint32 spawnTime = 6;
+		optional bool isAlive = 7;
+		optional uint32 deathTime = 8;
 	}
 
 	message Note {
-		required int32 type = 2;
+		optional int32 type = 2;
 		required float x = 3;
 		required float y = 4;
 	}
@@ -332,13 +332,13 @@ message AppMarker {
 	repeated AppMarker.SellOrder sellOrders = 13;
 
 	message SellOrder {
-		required int32 itemId = 1;
-		required int32 quantity = 2;
-		required int32 currencyId = 3;
-		required int32 costPerItem = 4;
-		required int32 amountInStock = 5;
-		required bool itemIsBlueprint = 6;
-		required bool currencyIsBlueprint = 7;
+		optional int32 itemId = 1;
+		optional int32 quantity = 2;
+		optional int32 currencyId = 3;
+		optional int32 costPerItem = 4;
+		optional int32 amountInStock = 5;
+		optional bool itemIsBlueprint = 6;
+		optional bool currencyIsBlueprint = 7;
 		optional float itemCondition = 8;
 		optional float itemConditionMax = 9;
 	}
